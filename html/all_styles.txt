
==== variables.css ====

:root {
  /* Цветовая палитра в стиле Linear/Vercel (Тёмная благородная тема) */
  --bg-dark: #051f20;
  --bg-dark-gray: #050505;
  --bg-card: #081214;
  --bg-card-icon: #0071e31a;
  --border-subtle: #ffffff14;
  --border-hover: #5ea9ff66;
  --icon-border-hover: #0071e333;

  --bg-shadow: #00000066;

  /* Текст */
  --text-primary: #f8f8f8;
  --text-secondary: #cad5d4;
  --text-muted: #666666;

  /* Акцентные цвета */
  --accent-blue: #5ea9ff;
  --accent-peach: #ff5e5e;
  --accent-blue-hover: #147ce5;
  --accent-blue-glow: #7fe7c614;
  --accent-success: #30d158;

  --accent-particles: #8eb69b8e;
  --accent-particles-rgb: 79, 209, 197;

  --card-spotlight-color: #0071e30f;

  --text-primary-rgb: 255, 255, 255;

  /* Шрифты и адаптив */
  --font-sans:
    "SF Pro Display", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
    Helvetica, Arial, sans-serif;

  /* Эффекты размытия и тени */
  --blur-premium: blur(20px);
  --blur-dropdown: blur(30px);
  --transition-smooth: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  --transition-fast: all 0.15s ease;

  /* Скругления углов */
  --radius-large: 19px;
  --radius-medium: 12px;
  --radius-capsule: 999px;
}

==== layout.css ====

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  background-color: var(--bg-dark);
  color: var(--text-primary);
  font-family: var(--font-sans);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  /* scroll-behavior: smooth; */
  /* overflow-x: hidden; */
  /* background:
    radial-gradient(
      1200px 700px at 50% -10%,
      rgba(60, 130, 255, 0.08),
      transparent 70%
    ),
    radial-gradient(
      900px 600px at 100% 30%,
      rgba(0, 170, 255, 0.04),
      transparent 80%
    ),
    linear-gradient(180deg, #072526 0%, #051f20 40%, #04191b 100%); */

  /* background-image:
    radial-gradient(
      circle at 50% 20%,
      rgba(54, 120, 255, 0.12),
      transparent 45%
    ),
    radial-gradient(circle at 80% 70%, rgba(0, 180, 255, 0.06), transparent 40%); */
}

body {
  min-height: 110vh;
  display: flex;
  flex-direction: column;
  overflow-x: hidden;
  position: relative;
}

/* Скроллбар в стиле macOS/iOS */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}
::-webkit-scrollbar-track {
  background: var(--bg-dark);
}
::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.15);
  border-radius: var(--radius-capsule);
}
::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, 0.3);
}

/* .main-container {
  flex: 1;
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding: 120px 24px 60px 24px; 
  Отступы с учетом плавающего навбара
} */

.text-center {
  text-align: center;
}

.text-left {
  text-align: left;
}

a {
  color: inherit;
  text-decoration: none;
}

/* Анимация при скролле для Intersection Observer */
.reveal {
  will-change: opacity, transform;
  opacity: 0;
  transform: translateY(20px);
  transition:
    opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1),
    transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
}

.reveal.fade-up {
  transform: translateY(40px);
}

/* Появление слева */
.reveal.fade-left {
  transform: translateX(-20px);
}

/* Появление справа */
.reveal.fade-right {
  transform: translateX(20px);
}

/* Мягкое увеличение */
.reveal.scale-up {
  transform: scale(0.92);
}

.reveal.revealed {
  opacity: 1;
  transform: translateY(0) scale(1);
}

/* Анимации */
.fade-in-page {
  will-change: opacity, transform;
  animation: pageReveal 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  opacity: 0;
}

@keyframes pageReveal {
  to {
    opacity: 1;
  }
}

.fade-in-left {
  will-change: opacity, transform;
  opacity: 0;
  animation: fadeLeft 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

@keyframes fadeLeft {
  from {
    opacity: 0;
    transform: translateX(-20px);
  }

  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.fade-in-right {
  will-change: opacity, transform;
  opacity: 0;
  animation: fadeRight 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

@keyframes fadeRight {
  from {
    opacity: 0;
    transform: translateX(20px);
  }

  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.fade-in-up {
  will-change: opacity, transform;
  opacity: 0;
  animation: fadeUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

@keyframes fadeUp {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.fade-in-down {
  will-change: opacity, transform;
  opacity: 0;
  animation: fadeDown 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

@keyframes fadeDown {
  from {
    opacity: 0;
    transform: translateY(20px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

==== navbar.css ====

.navbar-container {
  position: fixed;
  top: 20px;
  left: 50%;
  transform: translateX(-50%);
  width: calc(100% - 40px);
  max-width: 1100px;
  z-index: 1000;
  transition: var(--transition-smooth);
}

.navbar-inner {
  background: rgba(10, 10, 10, 0.4);
  backdrop-filter: var(--blur-premium);
  -webkit-backdrop-filter: var(--blur-premium);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-capsule);
  padding: 10px 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
  height: 56px;
}

.nav-left {
  display: flex;
  align-items: center;
  gap: 32px;
  position: relative;
}

.logo {
  display: flex;
  align-items: center;
  gap: 8px;
  font-weight: 600;
  font-size: 18px;
  letter-spacing: -0.02em;
  color: var(--text-primary);
}

.logo svg {
  color: var(--accent-blue);
  filter: drop-shadow(0 0 8px rgba(0, 113, 227, 0.4));
}

.nav-links-wrapper {
  position: relative;
  display: flex;
  align-items: center;
}

.nav-links {
  display: flex;
  align-items: center;
  gap: 4px;
  position: relative;
  z-index: 2;
}

.nav-link {
  font-size: 14px;
  font-weight: 500;
  color: var(--text-secondary);
  padding: 15px 14px;
  border-radius: var(--radius-capsule);
  transition: var(--transition-fast);
}

.nav-link.active {
  color: var(--text-primary);
}

/* Интерактивная капсула активного пункта */
.nav-capsule-indicator {
  position: absolute;
  height: 30px;
  background: rgba(255, 255, 255, 0.08);
  border-radius: var(--radius-capsule);
  transition: all 0.35s cubic-bezier(0.25, 1, 0.5, 1);
  z-index: 1;
  pointer-events: none;
  opacity: 0;
}

/* Стили выпадающего списка */
.dropdown-trigger {
  position: relative;
}

.dropdown-menu {
  position: absolute;
  top: calc(100% + 12px);
  left: 50%;
  transform: translateX(-50%) translateY(10px);
  background: rgba(15, 15, 15, 0.85);
  backdrop-filter: var(--blur-dropdown);
  -webkit-backdrop-filter: var(--blur-dropdown);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-large);
  width: 220px;
  padding: 8px;
  opacity: 0;
  pointer-events: none;
  transition:
    transform 0.2s cubic-bezier(0.16, 1, 0.3, 1),
    opacity 0.2s cubic-bezier(0.16, 1, 0.3, 1);
  box-shadow: 0 15px 40px rgba(0, 0, 0, 0.6);
  display: flex;
  flex-direction: column;
  gap: 2px;
  z-index: 100;
}

.dropdown-menu::before {
  content: "";
  position: absolute;
  top: -3px;
  left: 0;
  width: 100%;
  height: 3px;
}

.dropdown-menu a {
  font-size: 13px;
  font-weight: 500;
  color: var(--text-secondary);
  padding: 10px 14px;
  border-radius: var(--radius-medium);
  transition: var(--transition-fast);
}

.dropdown-menu a:hover {
  color: var(--text-primary);
  background: rgba(255, 255, 255, 0.05);
}

.dropdown-trigger:hover .dropdown-menu {
  opacity: 1;
  pointer-events: auto;
  transform: translateX(-50%) translateY(0);
}

/* Правая часть навбара */
.nav-right {
  display: flex;
  align-items: center;
  gap: 16px;
}

.nav-link-auth {
  font-size: 14px;
  font-weight: 500;
  color: var(--text-secondary);
  transition: var(--transition-fast);
}

.nav-link-auth:hover {
  color: var(--text-primary);
}

/* Статус-бейдж в навбаре */
.user-badge-nav {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 14px;
  font-weight: 500;
  padding: 6px 14px;
  background: rgba(255, 255, 255, 0.05);
  border-radius: var(--radius-capsule);
  border: 1px solid var(--border-subtle);
  transition: var(--transition-fast);
}

.user-badge-nav:hover {
  background: rgba(255, 255, 255, 0.1);
  border-color: rgba(255, 255, 255, 0.2);
}

.status-dot {
  width: 8px;
  height: 8px;
  background-color: var(--accent-success);
  border-radius: 50%;
  box-shadow: 0 0 8px var(--accent-success);
}

.hidden {
  display: none !important;
}

@media (max-width: 768px) {
  .nav-links-wrapper,
  .nav-right {
    display: none;
  }
  .mobile-menu-toggle {
    display: flex;
  }
}

==== buttons.css ====

/* Базовые стили кнопок в стиле Apple & Linear */
.btn-primary,
.btn-secondary,
.btn-linear-nav,
.mobile-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 14px;
  font-weight: 500;
  font-family: var(--font-sans);
  padding: 12px 24px;
  border-radius: var(--radius-large);
  cursor: pointer;
  transition: var(--transition-smooth);
  border: 1px solid transparent;
}

/* Главная акцентная кнопка (Apple Blue) */
.btn-primary {
  background: var(--accent-blue);
  color: var(--text-primary);
  box-shadow:
    0 4px 14px rgba(0, 113, 227, 0.3),
    inset 0 1px 0 rgba(255, 255, 255, 0.2);
}

.btn-primary:hover {
  background: var(--accent-blue-hover);
  transform: translateY(-1px);
  box-shadow:
    0 6px 20px rgba(0, 113, 227, 0.45),
    inset 0 1px 0 rgba(255, 255, 255, 0.2);
}

.btn-primary:active {
  transform: translateY(1px);
}

/* Второстепенная кнопка */
.btn-secondary {
  background: rgba(255, 255, 255, 0.05);
  border-color: var(--border-subtle);
  color: var(--text-primary);
}

.btn-secondary:hover {
  background: rgba(255, 255, 255, 0.08);
  border-color: rgba(255, 255, 255, 0.2);
  transform: translateY(-1px);
}

/* Кнопка регистрации в навбаре (Linear style) */
.btn-linear-nav {
  font-size: 13px;
  padding: 6px 14px;
  border-radius: var(--radius-capsule);
  background: linear-gradient(
    180deg,
    rgba(255, 255, 255, 0.1) 0%,
    rgba(255, 255, 255, 0.03) 100%
  );
  border: 1px solid var(--border-subtle);
  color: var(--text-primary);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

.btn-linear-nav:hover {
  border-color: rgba(255, 255, 255, 0.2);
  background: linear-gradient(
    180deg,
    rgba(255, 255, 255, 0.15) 0%,
    rgba(255, 255, 255, 0.05) 100%
  );
}

/* Мобильная кнопка */
.mobile-btn {
  width: 100%;
  background: var(--accent-blue);
  color: var(--text-primary);
  margin-top: 10px;
}

==== particles.css ====

.canvas-container {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: none;
  overflow: hidden;
}

#particles-canvas {
  display: block;
  width: 100%;
  height: 100%;
  opacity: 0.85;
}

==== forms.css ====

/* Авторизация в стиле Vercel / Linear */
.auth-container {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: calc(100vh - 200px);
    width: 100%;
}

.auth-card {
    background: var(--bg-card);
    backdrop-filter: var(--blur-premium);
    -webkit-backdrop-filter: var(--blur-premium);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-large);
    padding: 40px;
    width: 100%;
    max-width: 420px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
    text-align: center;
}

.auth-card h2 {
    font-size: 24px;
    font-weight: 600;
    margin-bottom: 24px;
    letter-spacing: -0.02em;
}

/* Поля ввода */
.input-modern {
    width: 100%;
    background: rgba(0, 0, 0, 0.2);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-medium);
    padding: 12px 16px;
    font-size: 14px;
    color: var(--text-primary);
    font-family: var(--font-sans);
    transition: var(--transition-smooth);
    margin-bottom: 16px;
    outline: none;
}

.input-modern::placeholder {
    color: var(--text-muted);
}

.input-modern:focus {
    border-color: var(--accent-blue);
    box-shadow: 0 0 0 3px var(--accent-blue-glow);
    background: rgba(0, 0, 0, 0.4);
}

/* Разделитель "ИЛИ" */
.auth-divider {
    display: flex;
    align-items: center;
    text-align: center;
    margin: 24px 0;
    color: var(--text-muted);
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

.auth-divider::before,
.auth-divider::after {
    content: '';
    flex: 1;
    border-bottom: 1px solid var(--border-subtle);
}

.auth-divider:not(:empty)::before {
    margin-right: 12px;
}

.auth-divider:not(:empty)::after {
    margin-left: 12px;
}

/* Кнопки OAuth */
.btn-outline {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    width: 100%;
    padding: 12px;
    background: transparent;
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-medium);
    color: var(--text-primary);
    font-size: 14px;
    font-weight: 500;
    cursor: pointer;
    transition: var(--transition-fast);
    margin-bottom: 12px;
}

.btn-outline:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(255, 255, 255, 0.2);
}

.auth-footer {
    margin-top: 24px;
    font-size: 13px;
    color: var(--text-secondary);
}

.auth-footer a {
    color: var(--accent-blue);
    text-decoration: none;
    font-weight: 500;
}

.auth-footer a:hover {
    text-decoration: underline;
}

/* Элементы Дашборда (Профиль) */
.dashboard-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 24px;
    margin-top: 32px;
}

.info-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    font-size: 14px;
}

.info-row:last-child {
    border-bottom: none;
}

.info-row span {
    color: var(--text-secondary);
}

.badge {
    padding: 4px 10px;
    border-radius: var(--radius-capsule);
    font-size: 12px;
    font-weight: 600;
}

.badge-success {
    background: rgba(48, 209, 88, 0.15);
    color: var(--accent-success);
    border: 1px solid rgba(48, 209, 88, 0.3);
}

/* Сетка клиентов (Быстрый старт) */
.clients-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
    gap: 16px;
    margin: 24px 0;
}

.client-app-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-medium);
    padding: 16px;
    text-align: center;
    transition: var(--transition-fast);
    cursor: pointer;
}

.client-app-card:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: var(--accent-blue);
    transform: translateY(-2px);
}
==== pages.css ====

/* Hero и контентные секции */
.hero-wrapper {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 140px 24px 80px 24px;
  position: relative;
  z-index: 1;
}

.hero-section {
  max-width: 800px;
  margin: 0 auto;
}

.hero-badge {
  display: inline-flex;
  align-items: center;
  padding: 6px 14px;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-capsule);
  font-size: 13px;
  font-weight: 500;
  color: var(--text-secondary);
  margin-bottom: 32px;
  letter-spacing: -0.01em;
}

.hero-title {
  font-size: 64px;
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.03em;
  margin-bottom: 24px;
  color: var(--text-primary);
}

/* Эффектный градиент для текста в стиле Stripe/Vercel */
.gradient-text {
  background: linear-gradient(135deg, #ffffff 30%, var(--accent-blue) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Эффект пролитой краски на тексте 
.gradient-text {
  position: relative;
  display: inline-block;

   Яркая "жидкая" заливочная палитра 
  background: linear-gradient(
    180deg,
    #ffffff 0%,
    var(--accent-blue) 45%,
    #00d2ff 85%,
    #0055ff 100%
  );
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;

   Подключаем SVG-фильтр деформации 
  filter: url(#paint-drip);
}

Дополнительный слой с эффектом "капель" и свечения сзади
.gradient-text::after {
  content: "в каждом клике.";
  position: absolute;
  left: 0;
  top: 0;
  z-index: -1;
  background: linear-gradient(180deg, transparent 40%, var(--accent-blue) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  filter: blur(12px) opacity(0.6);
  pointer-events: none;
} */

.hero-subtitle {
  font-size: 18px;
  line-height: 1.6;
  color: var(--text-secondary);
  max-width: 640px;
  margin: 0 auto 40px auto;
}

.hero-actions {
  display: flex;
  gap: 16px;
  justify-content: center;
}

/* Медленное кинематографичное проявление главного экрана */
.hero-section.reveal {
  transition:
    opacity 1.2s cubic-bezier(0.16, 1, 0.3, 1),
    transform 1.2s cubic-bezier(0.16, 1, 0.3, 1);
  transition-delay: 0ms;
}

/* Внутренние контейнеры страниц */
.page-container {
  max-width: 1100px;
  margin: 0 auto;
  padding: 140px 24px 80px 24px;
  position: relative;
  z-index: 2;
}

.section-title {
  font-size: 42px;
  font-weight: 700;
  letter-spacing: -0.02em;
  margin-bottom: 16px;
  text-align: center;
}

.section-subtitle {
  font-size: 16px;
  color: var(--text-secondary);
  text-align: center;
  max-width: 500px;
  margin: 0 auto 48px auto;
}

.link {
  color: var(--accent-blue);
  text-decoration: none;

  border-bottom-color: transparent;
  transition:
    color 0.2s ease,
    border-color 0.2s ease;
}

.link:hover {
  color: var(--accent-blue-hover);
  border-bottom: 1px solid currentColor;
}

==== contacts.css ====

/* ==========================================================================
   LINEAR-STYLE SPLIT GRID (Основной каркас для всех трех страниц)
   ========================================================================== */

.contacts-container {
  max-width: 90%;
  margin: 0 auto;
  padding: 140px 24px 80px 24px;
  position: relative;
  z-index: 2;
}

.linear-split-grid {
  display: grid;
  grid-template-columns: 1fr 1.1fr;
  gap: 80px;
  max-width: 1200px;
  margin: 60px auto 40px auto;
  align-items: start;
  padding: 0 24px;
}

/* Левая текстовая колонка */
.linear-hero-column {
  display: flex;
  flex-direction: column;
  position: sticky;
  /* position: static; */
  top: 140px; /*Фиксация контента при скролле правой части*/
}

.linear-hero-column.reveal {
  transition:
    opacity 1.2s cubic-bezier(0.16, 1, 0.3, 1),
    transform 1.2s cubic-bezier(0.16, 1, 0.3, 1);
  transition-delay: 0ms;
}

.linear-hero-column .hero-title {
  text-align: left;
  font-size: 48px;
  font-weight: 650;
  padding-bottom: 20px;
}

.linear-hero-column .hero-subtitle {
  margin-left: 0;
  max-width: 480px;
  text-align: left;
}

.linear-hero-column h3 {
  font-size: 18px;
  /* line-height: 1.6; */
  color: var(--text-secondary);
  margin-left: 0;
  max-width: 480px;
  text-align: left;
  font-weight: 240;
}

/* Переключатель контекста внизу левой колонки ("Связаться со службой поддержки" и т.д.) */
.linear-switch-context {
  margin-top: 40px;
  font-size: 14px;
  color: var(--text-secondary);
  line-height: 1.5;
}

.linear-switch-context a {
  color: var(--accent-blue);
  font-weight: 500;
  transition: var(--transition-fast);
}

.linear-switch-context a:hover {
  color: var(--text-primary);
  text-decoration: underline;
}

/* Правая колонка: контейнер для карточек хаба */
.linear-cards-column {
  display: flex;
  flex-direction: column;
  gap: 16px;
  align-items: flex-center;
}

/* ==========================================================================
   PAGE 1: contacts.html (Карточки выбора направления)
   ========================================================================== */

.linear-choice-card {
  background: var(--bg-card);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-large);
  padding: 32px;
  backdrop-filter: var(--blur-premium);
  -webkit-backdrop-filter: var(--blur-premium);
  transition: var(--transition-smooth);
  display: block;
  cursor: pointer;
  text-decoration: none;
  position: relative;
  overflow: hidden;
  max-width: 540px;
}

/* Изящная подсветка границ карточки Linear при наведении */
.linear-choice-card:hover {
  border-color: rgba(255, 255, 255, 0.2);
  transform: translateY(-2px);
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.3);
}

.linear-choice-card h3 {
  font-size: 20px;
  font-weight: 600;
  margin-bottom: 12px;
  color: var(--text-primary);
  transition: var(--transition-fast);
}

.linear-choice-card:hover h3 {
  color: var(--accent-blue);
}

.linear-choice-card p {
  font-size: 14px;
  line-height: 1.6;
  color: var(--text-secondary);
  margin: 0;
}

/* ==========================================================================
   PAGES 2 & 3: Оформление списков преимуществ (Левая колонка)
   ========================================================================== */

.linear-features-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.linear-features-list li {
  font-size: 15px;
  font-weight: 500;
  color: var(--text-primary);
  display: flex;
  align-items: center;
  gap: 16px;
}

/* Кастомный минималистичный чекбокс с галочкой */
.feature-icon-check {
  width: 20px;
  height: 20px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid var(--border-subtle);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  position: relative;
}

.feature-icon-check::after {
  content: "";
  width: 5px;
  height: 9px;
  border: solid var(--accent-blue);
  border-width: 0 2px 2px 0;
  transform: rotate(45deg);
  position: absolute;
  top: 3px;
}

/* ==========================================================================
   ФОРМЫ И БЛОКИ АВТОРИЗАЦИИ (Правая колонка)
   ========================================================================== */

/* Обертка формы */
.contact-form-wrapper {
  background: var(--bg-card);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-large);
  padding: 40px;
  backdrop-filter: var(--blur-premium);
  -webkit-backdrop-filter: var(--blur-premium);
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.4);
  max-width: 480px;
}

/* Тексты внутри инпутов и форм */
.form-instruction-title {
  font-size: 18px;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 28px;
  letter-spacing: -0.01em;
}

.form-group-modern {
  margin-bottom: 20px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.form-label-modern {
  font-size: 13px;
  font-weight: 500;
  color: var(--text-secondary);
}

/* Кнопка отправки и сноска под ней */
.form-actions-modern {
  margin-top: 32px;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.linear-submit-btn {
  width: 100%;
  padding: 14px;
  font-weight: 600;
}

.form-alternative-note {
  font-size: 12px;
  color: var(--text-muted);
  line-height: 1.5;
  text-align: center;
}

.form-alternative-note a {
  color: var(--text-secondary);
  text-decoration: underline;
}

.form-alternative-note a:hover {
  color: var(--text-primary);
}

/* PAGE 3 SPECIFIC: Карточка ограничения доступа (Заглушка авторизации) */
.linear-auth-gate {
  display: flex;
  flex-direction: column;
}

.linear-gate-box {
  background: rgba(0, 0, 0, 0.2);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-medium);
  padding: 32px 24px;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
}

.linear-gate-box p {
  font-size: 14px;
  color: var(--text-secondary);
  line-height: 1.6;
  margin: 0;
  max-width: 280px;
}

.linear-gate-btn {
  width: 100%;
  max-width: 200px;
  padding: 10px 20px;
  font-weight: 600;
}

/* ==========================================================================
   СИМУЛЯЦИЯ ИНТЕРАКТИВНОГО ЧАТА (Оставляем твою рабочую логику)
   ========================================================================== */

.chat-simulation {
  display: flex;
  flex-direction: column;
  gap: 16px;
  height: 100%;
}

.chat-simulation.show {
  display: flex;
  animation: fadeIn 0.4s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

.chat-bubble {
  max-width: 85%;
  padding: 14px 18px;
  border-radius: var(--radius-large);
  font-size: 14px;
  line-height: 1.5;
}

.chat-bubble.bot {
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid var(--border-subtle);
  align-self: flex-start;
  color: var(--text-primary);
  border-bottom-left-radius: 4px;
}

.chat-bubble.user {
  background: var(--accent-blue);
  color: #ffffff;
  align-self: flex-end;
  border-bottom-right-radius: 4px;
}

/* Индикатор набора текста ботом */
.typing-indicator {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 14px 18px;
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-large);
  border-bottom-left-radius: 4px;
  align-self: flex-start;
}

.typing-dot {
  width: 6px;
  height: 6px;
  background: var(--text-secondary);
  border-radius: 50%;
  opacity: 0.4;
  animation: typing 1s infinite ease-in-out;
}

.typing-dot:nth-child(2) {
  animation-delay: 0.2s;
}
.typing-dot:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes typing {
  0%,
  100% {
    transform: translateY(0);
    opacity: 0.4;
  }
  50% {
    transform: translateY(-4px);
    opacity: 1;
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* ==========================================================================
   АДАПТИВНОСТЬ (Responsive design)
   ========================================================================== */

@media (max-width: 960px) {
  .linear-split-grid {
    grid-template-columns: 1fr;
    gap: 48px;
    margin-top: 40px;
  }

  .linear-hero-column {
    position: static;
  }

  .linear-switch-context {
    margin-top: 24px;
  }
}
